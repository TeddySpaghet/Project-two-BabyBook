<div class="container">
<h4>Welcome to First Steps, your home for tracking all your precious memories</h4>

<h6>Create a profile for your baby to get started. </h6>

</div>
<!-- Add description of what the s -->







    <!-- start of image uploader code -->
    <!-- <div class="container">
      <h2>File upload</h2>
      <%= //typeof msg != 'undefined' ? msg : '' %> 
      <form action="/upload" method="POST" enctype="multipart/form-data">
        <div class="file-field input-field">
          <div class="btn grey">
            <span>File</span>
            <input name="newImage" type="file">
          </div>
          <div class="file-path-wrapper">
            <input class="file-path validate" type="text">
          </div>
        </div>
        <button type="submit" class="btn">Upload</button>
      </form>
      <br>
      <!--  typeof 'file' comes from image post route on server.js -->
      <!-- <img src="<%= //typeof file != 'undefined' ? file : '' %>" class="responsive-img"alt="Uploaded image not working"> -->
      <!-- <img src="<%= //typeof file != 'undefined' ? file : '' %>" alt="Uploaded image not working">
      <img src="images/05month.jpg">
      
    </div> -->
    <!-- end of image uploader code -->